# walk_env + gym 등록 + 스모크 구현 계획 (이슈 #12, 뼈대 C)

> 태스크를 순서대로 하나씩 수행하고, 각 단계의 `- [ ]` 체크박스로 진행을 추적한다. 각 태스크는 실패하는 테스트 → 구현 → 통과 → 커밋의 사이클로 닫힌다.

**Goal:** `Chair-Walk-Direct-v0`가 `gym.make`로 뜨고, 16 env × 50 스텝을 NaN 없이 돌며, 리셋 직후 관측이 규약값이고, `a2j` 매핑으로 정책 인덱스가 올바른 관절을 움직인다.

**Architecture:** `walk_env.py` 하나에 `WalkEnvCfg(DirectRLEnvCfg)` + `WalkEnv(DirectRLEnv)`. env 는 이슈 A(`chair_asset`)·B(`obs_layout`, `mdp`)의 순수 함수를 부르는 얇은 껍데기다 — 상태(이력 버퍼·potentials·작용 전 쿼터니언)만 들고 수식은 갖지 않는다. `base_env.py` 는 만들지 않는다: 두 번째 소비자(`stand_env`)가 없는 상태에서 공통 베이스를 뽑으면 이음매를 추측으로 긋게 되므로, `stand_env` 이슈에서 실제 차이를 보고 추출한다(브레인스토밍 2026-08-27 결정).

**Tech Stack:** Isaac Lab 0.54 (`DirectRLEnv`), gymnasium, torch, pytest. 스모크 테스트는 Kit 필요(`--isaac`).

**Spec:** `docs/specs/2026-08-25-isaac-rl-design.md` — §1(등록), §3(관측·타이밍 지연), §5 walk(초기 상태), §9.1(단위), §9.3(데이터 흐름), §9.4(종료 기하), §9.5(Cfg), §9.6(테스트). 이슈: https://github.com/Tonnonssi/SOTA/issues/12

## Global Constraints

- 브랜치 `feat/12-walk-env`, 이슈 #12 하나에 대응. (CLAUDE.md)
- diff가 400줄을 넘으면 멈추고 분할안을 제시한다. 예상 ~370. **Task 3 끝에서 재고, 넘으면 Task 1–2(A_STAND·등록·env·리셋/스텝 스모크)와 Task 3(a2j·종료-리셋·타이밍 짝 테스트)를 커밋 경계로 두 PR로 나눈다.** (CLAUDE.md)
- 계획 밖의 문제는 고치지 말고 멈추고 보고 — 라벨: 깊이초과 / 선행조건 / 전제무효화 / 인접유혹. (CLAUDE.md)
- PR 본문에 "이 코드가 틀렸다면 어떻게 틀렸을지" 필수. (CLAUDE.md)
- **`DirectRLEnv.step()` 훅 순서는 소스 그대로다** (`~/IsaacLab/source/isaaclab/isaaclab/envs/direct_rl_env.py:333–418`): `_pre_physics_step` → ×decimation{`_apply_action`, `write_data_to_sim`, `sim.step`, `scene.update`} → `episode_length_buf += 1` → `_get_dones` → `_get_rewards` → `_reset_idx(reset_env_ids)` → `_get_observations`. `reset()` 은 `_reset_idx(all)` → `_get_observations`. 이 순서가 곧 정확성이다 (§9.3).
- **이력의 (액션, 쿼터니언) 짝 = 실기 순서.** `_pre_physics_step` 에서 `root_quat_w` 를 **clone** 해 두고 `_get_observations` 가 그 "작용 전" 쿼터니언을 push 한다. 실기 `rl_walk.py` 는 a_t 를 publish 한 직후 IMU 를 읽으므로 index 0 = (a_t, a_t 작용 전 q). (§3 타이밍 지연, 이슈 C 결정)
- **이력의 액션은 클립 전(raw), 관절 목표는 클립 후.** `ACTION_LIMIT = 0.872665`. (§9.3)
- **리셋된 env 의 첫 관측은 리셋값 그대로.** `push(..., skip_mask=…)`. `reset()` 경로(전체 리셋)에서도 성립해야 하므로 `reset_buf` 가 아니라 env 가 드는 `_skip_push` 마스크를 쓴다 — `_reset_idx` 가 켜고 `_get_observations` 가 쓰고 끈다. (§9.3, §9.6)
- **보상·종료의 위치는 env 로컬 좌표.** `p = root_pos_w − scene.env_origins`. `p_target = (10,0,0)` 은 env 원점 기준이지 월드 기준이 아니다 — 월드 좌표를 쓰면 env 마다 목표 방향이 달라진다. 쿼터니언은 월드 그대로(원점 이동은 회전에 무관). (§4, §9.3)
- **관절 순서:** 정책 인덱스 → 관절 인덱스 `a2j` 는 `robot.find_joints(POLICY_JOINT_NAMES, preserve_order=True)` 한 곳에서만 나오고, `set_joint_position_target(target, joint_ids=a2j)` 로 넘긴다. §9.3 의 `self._act[:, a2j]` 표기는 방향이 반대로 읽히므로 쓰지 않는다. (§9.3)
- **초기 상태 (§5 walk):** 관절 `a_stand ± 0.02 rad`, 좌면 중심 z `0.101 ± 0.002 m`, roll/pitch `± 0.02 rad`, **yaw 0 고정**. 전부 cfg 필드. 0.101 은 §3 실측(서 있을 때 `dummy z = 0.101`).
- **시간:** `sim.dt = 1/120`, `decimation = 12` → `step_dt = 0.1 = mdp.CONTROL_DT`; `episode_length_s = 35.0` → `max_episode_length = 350 = mdp.MAX_EPISODE_LEN`. env `__init__` 에서 둘 다 assert 한다 — 단일 출처는 `mdp` 상수. (§9.5, 이슈 #6 결정)
- **gym 등록은 `env_cfg_entry_point` 만.** entry point 는 문자열(`"chair_rl.walk_env:WalkEnv"`)이라 `import chair_rl` 이 Isaac 을 끌어오지 않는다 — CPU 테스트(`test_mass_spec` 은 lerobot 파이썬)가 계속 돈다. agent cfg 는 학습 이슈. (§9.5 축소, 브레인스토밍 결정)
- **범위 밖:** `stand_env`/`base_env`, `agents/`, `scripts/rsl_rl/train.py`, 도메인 랜덤화·노이즈·액션 지연(§6), 서보 2주기 지연 보정, `chair_sim.py` 수정(자체 `STANDING_SIM` 중복은 인접유혹 — 손대지 않는다).
- 실행 환경: conda `env_isaaclab` 활성화, 저장소 루트에서 `pytest isaac -q`(CPU) / `pytest isaac --isaac -q`(Kit). ROS 가 sourced 된 셸이면 `PYTHONPATH` 를 비운다. GPU: RTX 4090 — 스모크는 cfg 기본 `cuda:0`.
- 커밋 `feat[isaac]: …` / `test[isaac]: …`, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## Isaac Lab API (소스 확인, 0.54.4)

| 호출 | 시그니처 / 반환 | 위치 |
|---|---|---|
| `Articulation.find_joints(name_keys, joint_subset=None, preserve_order=False)` | `(list[int], list[str])` | `assets/articulation/articulation.py:288` |
| `Articulation.set_joint_position_target(target, joint_ids=None, env_ids=None)` | — | `:1079` |
| `Articulation.write_root_pose_to_sim(root_pose (K,7) wxyz, env_ids)` / `write_root_velocity_to_sim((K,6), env_ids)` | — | `:399`, `:485` |
| `Articulation.write_joint_state_to_sim(position, velocity, joint_ids=None, env_ids=None)` | — | `:561` |
| `robot.data.root_pos_w (N,3)`, `root_quat_w (N,4) (w,x,y,z)`, `joint_pos`, `joint_vel`, `joint_pos_target`, `default_joint_pos`, `default_root_state (N,13)` | — | `articulation_data.py` |
| `scene.env_origins (N,3)` | — | `scene/interactive_scene.py:367` |
| `sample_uniform(lower, upper, size, device)` | `torch.rand·(u−l)+l` | `utils/math.py:1401` |
| `quat_from_euler_xyz(roll, pitch, yaw)` | `(N,4) (w,x,y,z)` | `utils/math.py:274` |
| `DirectRLEnv` 버퍼: `episode_length_buf`, `reset_terminated`, `reset_time_outs`, `reset_buf`, `extras`, `max_episode_length`, `step_dt`, `num_envs`, `device` | — | `envs/direct_rl_env.py:140–300` |
| `DirectRLEnv.close()` → `sim.clear_all_callbacks(); sim.clear_instance()` | — | `:504` |
| `gym.make(id, cfg=…)` → 등록 kwargs(`env_cfg_entry_point`)가 `**kwargs` 로 함께 넘어온다 | — | gymnasium 1.2 |

## 파일 구조

| 파일 | 책임 | 만듦/고침 |
|---|---|---|
| `isaac/chair_rl/mdp.py` | `A_STAND` 상수 추가 (논문 Table VI `a_stand` = 실기 `STANDING_POS` 역변환) | 고침 (+6) |
| `isaac/tests/test_mdp.py` | `A_STAND` ≡ `keyframes.real_deg_to_sim_rad(STANDING_POS)` CPU 테스트 | 고침 (+12) |
| `isaac/chair_rl/__init__.py` | `gym.register("Chair-Walk-Direct-v0")` | 고침 (+15) |
| `isaac/tests/test_registry.py` | 등록이 Isaac 없이 성립하는지 CPU 테스트 | 만듦 (~12) |
| `isaac/pyproject.toml` | `dependencies` 에 `gymnasium` | 고침 (+1) |
| `isaac/chair_rl/walk_env.py` | `WalkEnvCfg`, `WalkEnv` (훅 6개 + `_setup_scene`) | 만듦 (~190) |
| `isaac/tests/test_env_smoke.py` | `@pytest.mark.isaac` 스모크 | 만듦 (~130) |

---

### Task 1: `mdp.A_STAND` + gym 등록 (CPU 테스트)

**Files:**
- Modify: `isaac/chair_rl/mdp.py` (상수 블록, `MAX_EPISODE_LEN` 아래)
- Modify: `isaac/chair_rl/__init__.py` (현재 빈 파일)
- Modify: `isaac/pyproject.toml:10`
- Create: `isaac/tests/test_registry.py`
- Test: `isaac/tests/test_mdp.py` (함수 1개 추가)

**Interfaces:**
- Produces (Task 2가 쓴다):
  - `mdp.A_STAND: tuple[float, ×6]` — 정책 인덱스 순서(`POLICY_JOINT_NAMES`)의 서 있는 자세, rad.
  - gym id `"Chair-Walk-Direct-v0"`, entry point `"chair_rl.walk_env:WalkEnv"`, kwargs `{"env_cfg_entry_point": "chair_rl.walk_env:WalkEnvCfg"}`.

- [ ] **Step 1: 실패하는 테스트 작성**

`isaac/tests/test_mdp.py` 끝에 추가:

```python
def test_a_stand_matches_real_robot_standing_pose():
    """논문 Table VI a_stand = [-0.1745, 0, -0.1745, 0, 0.1745, 0] 가 실기 config.STANDING_POS 의
    sim 변환값과 같다 (설계문서 §4 "세 가지 함의" 1). keyframes.py 는 isaac/ 에 있고 conftest 가
    isaac/ 를 sys.path 에 넣으므로 여기서 import 된다."""
    import keyframes

    sim = keyframes.real_deg_to_sim_rad(keyframes.safe_clip(keyframes.cfg.STANDING_POS))[0]
    np.testing.assert_allclose(mdp.A_STAND, sim, atol=1e-6)
    np.testing.assert_allclose(mdp.A_STAND, [-0.1745, 0.0, -0.1745, 0.0, 0.1745, 0.0], atol=1e-4)
```

(`test_mdp.py` 상단에 `import numpy as np` 가 없으면 추가한다. `mdp` 는 이미 `from chair_rl import mdp` 로 들어와 있다 — 아니면 그 형태로 맞춘다.)

`isaac/tests/test_registry.py`:

```python
"""gym 등록이 Isaac 없이 성립하는지. entry point 가 문자열이므로 import chair_rl 은 Kit 을 띄우지 않는다."""

import gymnasium as gym

import chair_rl  # noqa: F401  — 등록 부수효과


def test_walk_task_registered_lazily():
    spec = gym.spec("Chair-Walk-Direct-v0")
    assert spec.entry_point == "chair_rl.walk_env:WalkEnv"
    assert spec.kwargs["env_cfg_entry_point"] == "chair_rl.walk_env:WalkEnvCfg"
    assert spec.disable_env_checker is True
```

- [ ] **Step 2: 실패 확인**

```bash
pytest isaac/tests/test_mdp.py::test_a_stand_matches_real_robot_standing_pose isaac/tests/test_registry.py -v
```

Expected: `AttributeError: module 'chair_rl.mdp' has no attribute 'A_STAND'`; `gymnasium.error.NameNotFound: Environment Chair-Walk-Direct doesn't exist`.

- [ ] **Step 3: 구현**

`isaac/chair_rl/mdp.py` — `MAX_EPISODE_LEN` 줄 아래에:

```python
# 논문 Table VI 의 a_stand = [-0.1745, 0, -0.1745, 0, 0.1745, 0] (정책 인덱스 순서).
# 실기 config.STANDING_POS [90,80,90,100,90,100] 의 역변환 deg2rad(90 - real[5-j]) 과 일치한다
# (설계문서 §4) — walk 의 초기 자세이자 stand 의 목표 자세. 정확값은 ±10° 다.
_DEG10 = math.radians(10.0)
A_STAND = (-_DEG10, 0.0, -_DEG10, 0.0, _DEG10, 0.0)
```

(`mdp.py` 상단에 `import math` 추가.)

`isaac/chair_rl/__init__.py`:

```python
"""chair_rl — Chair-type tripedal robot, Isaac Lab RL env (설계문서 §1).

import 만으로 gym 태스크가 등록된다. entry point 는 문자열이라 여기서는 Isaac 을 import 하지
않는다 — obs_layout/mdp/mass_spec 의 CPU 테스트가 Kit 없이 계속 돈다 (§9.1).
agent cfg(rsl_rl/rl_games) entry point 는 학습 이슈에서 건다 (§9.5 축소, 이슈 #12).
"""

import gymnasium as gym

gym.register(
    id="Chair-Walk-Direct-v0",
    entry_point="chair_rl.walk_env:WalkEnv",
    disable_env_checker=True,
    kwargs={"env_cfg_entry_point": "chair_rl.walk_env:WalkEnvCfg"},
)
```

`isaac/pyproject.toml`: `dependencies = ["numpy", "torch"]` → `dependencies = ["numpy", "torch", "gymnasium"]`.

- [ ] **Step 4: 통과 확인 + 전체 CPU**

```bash
pytest isaac/tests/test_mdp.py isaac/tests/test_registry.py -v
pytest isaac -q
```

Expected: 새 테스트 2개 PASS, 전체 = 이전 + 2, Isaac 테스트는 skip. `import chair_rl` 이 lerobot 파이썬에서도 되는지 한 번: `~/miniforge3/envs/lerobot/bin/python -m pytest isaac/tests/test_mass_spec.py -q` (gymnasium 1.3.0 있음 확인됨).

- [ ] **Step 5: Commit**

```bash
git add isaac/chair_rl/mdp.py isaac/chair_rl/__init__.py isaac/pyproject.toml isaac/tests/test_mdp.py isaac/tests/test_registry.py
git commit -m "feat[isaac]: mdp.A_STAND (논문 a_stand ≡ 실기 STANDING_POS) 와 Chair-Walk-Direct-v0 lazy 등록

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `walk_env.py` — Cfg + env, 리셋/스텝 스모크 (Kit)

**Files:**
- Create: `isaac/chair_rl/walk_env.py`
- Test: `isaac/tests/test_env_smoke.py` (이 태스크에서 픽스처 + 테스트 3개; Task 3 이 3개 추가)

**Interfaces:**
- Consumes: `chair_asset.build_usd(spec) -> str`, `chair_asset.articulation_cfg(usd_path, prim_path, spawn_height, joint_pos, effort_limit)`, `mass_spec.MUJOCO`/`.with_servos()`, `obs_layout.{new_history, reset_history, push, wxyz_to_xyzw, ACTION_LIMIT, POLICY_JOINT_NAMES, OBS_DIM, NUM_ACTIONS}`, `mdp.{A_STAND, CONTROL_DT, MAX_EPISODE_LEN, P_TARGET, WalkRewardWeights, walk_reward_terms, walk_total, walk_terminated, walk_truncated, potential}`.
- Produces (Task 3 테스트와 학습 이슈가 쓴다):
  - `WalkEnvCfg(DirectRLEnvCfg)` 필드: `decimation=12`, `episode_length_s=35.0`, `observation_space=40`, `action_space=6`, `state_space=0`, `sim=SimulationCfg(dt=1/120, render_interval=12)`, `scene=InteractiveSceneCfg(num_envs=4096, env_spacing=0.6)`, `servo_mass=False`, `effort_limit=0.3`, `init_height=0.101`, `init_height_noise=0.002`, `init_joint_noise=0.02`, `init_tilt_noise=0.02`, `reward_weights=mdp.WalkRewardWeights()`.
  - `WalkEnv(DirectRLEnv)` 속성: `robot: Articulation`, `_a2j: list[int]`, `_rot_his (N,4,4)`, `_act_his (N,4,6)`, `_raw_act (N,6)`, `_act (N,6)`, `_quat_pre (N,4) wxyz`, `_potentials (N,)`, `_skip_push (N,) bool`, `_term_reasons: dict`. 메서드 `root_pos_local() -> (N,3)`.
  - `obs = {"policy": (N,40)}`, `extras["log"]` 키: `rew/{progress,height,up,heading,action,vel}`, `term/{tilt,ground,height}`.

- [ ] **Step 1: 실패하는 테스트 작성**

`isaac/tests/test_env_smoke.py`:

```python
"""Chair-Walk-Direct-v0 스모크 (설계문서 §9.6). Kit 필요:
    pytest isaac/tests/test_env_smoke.py --isaac -v

env 는 모듈당 하나만 띄운다 — DirectRLEnv 는 SimulationContext 가 이미 있으면 거부하고,
test_asset_build 도 같은 세션에서 자기 컨텍스트를 만들고 지우므로 close() 로 확실히 비운다.
"""

import numpy as np
import pytest
import torch

from chair_rl import mdp
from chair_rl import obs_layout as ol

pytestmark = pytest.mark.isaac

NUM_ENVS = 16


@pytest.fixture(scope="module")
def env(kit_app):
    import gymnasium as gym

    import chair_rl  # noqa: F401  — 등록
    from chair_rl.walk_env import WalkEnvCfg

    cfg = WalkEnvCfg()
    cfg.scene.num_envs = NUM_ENVS
    cfg.seed = 0
    wrapped = gym.make("Chair-Walk-Direct-v0", cfg=cfg)
    yield wrapped.unwrapped
    wrapped.close()


def _rand_actions(env, lo=-ol.ACTION_LIMIT, hi=ol.ACTION_LIMIT):
    return torch.rand(env.num_envs, ol.NUM_ACTIONS, device=env.device) * (hi - lo) + lo


def test_timing_matches_mdp_constants(env):
    assert env.step_dt == pytest.approx(mdp.CONTROL_DT)
    assert env.max_episode_length == mdp.MAX_EPISODE_LEN == 350


def test_reset_obs_is_history_init(env):
    obs, _ = env.reset()
    o = obs["policy"]
    assert o.shape == (NUM_ENVS, ol.OBS_DIM)
    rot = o[:, :16].view(NUM_ENVS, 4, 4)
    assert torch.all(rot[..., :3] == 0.0) and torch.all(rot[..., 3] == 1.0)
    assert torch.all(o[:, 16:] == ol.ACT_INIT)
    # potentials 는 env 로컬 좌표로 계산돼야 한다 — 월드 좌표면 env 원점(spacing 0.6 m)만큼 서로 다르다
    pot = env._potentials.cpu().numpy()
    expect = -np.linalg.norm(np.array(mdp.P_TARGET) - np.array([0.0, 0.0, env.cfg.init_height])) / mdp.CONTROL_DT
    np.testing.assert_allclose(pot, expect, atol=0.1)


def test_50_random_steps_finite(env):
    env.reset()
    for _ in range(50):
        obs, rew, terminated, truncated, extras = env.step(_rand_actions(env))
        o = obs["policy"]
        assert o.shape == (NUM_ENVS, ol.OBS_DIM) and torch.isfinite(o).all()
        assert rew.shape == (NUM_ENVS,) and torch.isfinite(rew).all()
        assert terminated.dtype == torch.bool and truncated.dtype == torch.bool
        assert not truncated.any()  # 50 < 350
    log = extras["log"]
    assert {f"rew/{k}" for k in ("progress", "height", "up", "heading", "action", "vel")} <= set(log)
    assert {f"term/{k}" for k in ("tilt", "ground", "height")} <= set(log)
    # 관절이 실제로 움직였다 (a2j 로 지령이 전달됨)
    assert (env.robot.data.joint_pos - env.robot.data.default_joint_pos).abs().max() > 0.1
```

- [ ] **Step 2: 실패 확인**

```bash
pytest isaac/tests/test_env_smoke.py --isaac -v
```

Expected: 픽스처에서 `ModuleNotFoundError: No module named 'chair_rl.walk_env'` (gym.make 가 entry point 를 import 하는 시점).

- [ ] **Step 3: 구현**

`isaac/chair_rl/walk_env.py`:

```python
"""Chair-Walk-Direct-v0 — 걷기 DirectRLEnv (설계문서 §9.3, §9.5).

env 는 얇은 껍데기다: 상태(이력 버퍼·potentials·작용 전 쿼터니언)만 들고, 수식은 전부
obs_layout / mdp 의 순수 함수에 있다. 훅 순서는 DirectRLEnv.step() 소스 그대로:
  _pre_physics_step → ×12 _apply_action → _get_dones → _get_rewards → _reset_idx → _get_observations

실기 rl_walk.py 와 맞춘 세 가지 (§3, §9.3):
  1. 이력 index 0 = (a_t, a_t 작용 *전* 쿼터니언). 실기는 publish 직후 IMU 를 읽는다.
     그래서 _pre_physics_step 이 root_quat_w 를 clone 해 두고 _get_observations 가 그걸 push 한다.
  2. 이력의 액션은 클립 전 raw, 관절 목표만 ±ACTION_LIMIT 클립.
  3. 리셋된 env 의 첫 관측은 리셋값 그대로 (push 건너뜀). reset() 경로에서도 성립하도록
     reset_buf 가 아니라 _skip_push 마스크를 쓴다.

위치는 env 로컬(p = root_pos_w − env_origins). p_target=(10,0,0) 은 env 원점 기준이다.
base_env 추출은 stand_env 이슈에서 (이슈 #12 결정).
"""

from __future__ import annotations

from collections.abc import Sequence

import torch

import isaaclab.sim as sim_utils
from isaaclab.assets import Articulation
from isaaclab.envs import DirectRLEnv, DirectRLEnvCfg
from isaaclab.scene import InteractiveSceneCfg
from isaaclab.sim import SimulationCfg
from isaaclab.utils import configclass
from isaaclab.utils.math import quat_from_euler_xyz, sample_uniform

from . import chair_asset, mdp
from . import obs_layout as ol
from .mass_spec import MUJOCO


@configclass
class WalkEnvCfg(DirectRLEnvCfg):
    # 시간: 1/120 × 12 = 0.1 s = mdp.CONTROL_DT, 35 s / 0.1 = 350 = mdp.MAX_EPISODE_LEN (§9.5, §2⑤)
    decimation = 12
    episode_length_s = mdp.MAX_EPISODE_LEN * mdp.CONTROL_DT
    sim: SimulationCfg = SimulationCfg(dt=1.0 / 120.0, render_interval=decimation)
    observation_space = ol.OBS_DIM
    action_space = ol.NUM_ACTIONS
    state_space = 0
    scene: InteractiveSceneCfg = InteractiveSceneCfg(num_envs=4096, env_spacing=0.6, replicate_physics=True)

    # 에셋 (§9.2): 질량 스펙은 MuJoCo 그대로가 기준선, 서보 54 g 은 §2 system ID 의 실험 변수
    servo_mass = False
    effort_limit = 0.3

    # 초기 상태 (§5 walk): yaw 는 랜덤화하지 않는다 — p_target/heading 이 +x 고정
    init_height = 0.101          # 서 있을 때 dummy z 실측 (§3)
    init_height_noise = 0.002
    init_joint_noise = 0.02
    init_tilt_noise = 0.02       # roll, pitch 각각

    # 보상 가중치 (Table IV) — 종료 임계값은 mdp 상수 (논문값, 튜닝 대상 아님)
    reward_weights: mdp.WalkRewardWeights = mdp.WalkRewardWeights()


class WalkEnv(DirectRLEnv):
    cfg: WalkEnvCfg

    def __init__(self, cfg: WalkEnvCfg, render_mode: str | None = None, **kwargs):
        super().__init__(cfg, render_mode, **kwargs)
        # 시간 상수의 단일 출처는 mdp 다 (이슈 #6). cfg 가 어긋나면 progress 의 dt 와 truncation 이 틀어진다.
        assert abs(self.step_dt - mdp.CONTROL_DT) < 1e-9, self.step_dt
        assert self.max_episode_length == mdp.MAX_EPISODE_LEN, self.max_episode_length

        # 정책 인덱스 → 관절 인덱스. 아티큘레이션 순서는 [joint2, joint4, joint6, joint1, joint3, joint5]
        # (임포터의 폭우선 순회, §9.3) 이라 정책 순서와 다르다. 이 한 곳에서만 만든다.
        self._a2j, names = self.robot.find_joints(list(ol.POLICY_JOINT_NAMES), preserve_order=True)
        assert tuple(names) == ol.POLICY_JOINT_NAMES, names

        n, dev = self.num_envs, self.device
        self._a_stand = torch.tensor(mdp.A_STAND, device=dev)
        self._p_target = torch.tensor(mdp.P_TARGET, device=dev)
        self._rot_his, self._act_his = ol.new_history(n, dev)
        self._raw_act = torch.full((n, ol.NUM_ACTIONS), ol.ACT_INIT, device=dev)
        self._act = self._raw_act.clamp(-ol.ACTION_LIMIT, ol.ACTION_LIMIT)
        self._quat_pre = torch.zeros(n, 4, device=dev)
        self._quat_pre[:, 0] = 1.0                       # (w,x,y,z) 단위
        self._potentials = torch.zeros(n, device=dev)
        self._skip_push = torch.zeros(n, dtype=torch.bool, device=dev)
        self._term_reasons: dict[str, torch.Tensor] = {}

    # ---------------------------------------------------------------- scene

    def _setup_scene(self):
        spec = MUJOCO.with_servos() if self.cfg.servo_mass else MUJOCO
        usd_path = chair_asset.build_usd(spec)
        joint_pos = {name: float(mdp.A_STAND[i]) for i, name in enumerate(ol.POLICY_JOINT_NAMES)}
        self.robot = Articulation(chair_asset.articulation_cfg(
            usd_path, prim_path="/World/envs/env_.*/Robot",
            spawn_height=self.cfg.init_height, joint_pos=joint_pos,
            effort_limit=self.cfg.effort_limit,
        ))
        ground = sim_utils.GroundPlaneCfg()
        ground.func("/World/ground", ground)
        self.scene.clone_environments(copy_from_source=False)
        if self.device == "cpu":
            self.scene.filter_collisions(global_prim_paths=[])
        self.scene.articulations["robot"] = self.robot
        light = sim_utils.DomeLightCfg(intensity=2500.0, color=(0.9, 0.9, 0.92))
        light.func("/World/light", light)

    # ---------------------------------------------------------------- helpers

    def root_pos_local(self) -> torch.Tensor:
        """좌면 중심(dummy) 위치, env 원점 기준 (N,3)."""
        return self.robot.data.root_pos_w - self.scene.env_origins

    def _root_quat_xyzw(self) -> torch.Tensor:
        return ol.wxyz_to_xyzw(self.robot.data.root_quat_w)

    # ---------------------------------------------------------------- hooks (순서 = step())

    def _pre_physics_step(self, actions: torch.Tensor):
        self._raw_act = actions.clone()                                   # 이력용 (클립 전)
        self._act = actions.clamp(-ol.ACTION_LIMIT, ol.ACTION_LIMIT)      # 관절용
        self._quat_pre = self.robot.data.root_quat_w.clone()              # clone 필수 — 뷰면 물리 뒤 덮인다

    def _apply_action(self):
        self.robot.set_joint_position_target(self._act, joint_ids=self._a2j)

    def _get_dones(self) -> tuple[torch.Tensor, torch.Tensor]:
        terminated, self._term_reasons = mdp.walk_terminated(self.root_pos_local(), self._root_quat_xyzw())
        truncated = mdp.walk_truncated(self.episode_length_buf)
        return terminated, truncated

    def _get_rewards(self) -> torch.Tensor:
        # prev_actions = 이력 index 0 = 직전 스텝의 raw 액션 (push 는 _get_observations 에서, 이 뒤에)
        terms, self._potentials = mdp.walk_reward_terms(
            self.root_pos_local(), self._root_quat_xyzw(), self._potentials,
            self._raw_act, self._act_his[:, 0], self.robot.data.joint_vel,
            dt=mdp.CONTROL_DT, p_target=self._p_target,
        )
        reward = mdp.walk_total(terms, self.cfg.reward_weights, self.reset_terminated)
        log = {f"rew/{k}": v.mean() for k, v in terms.items()}
        log.update({f"term/{k}": v.float().mean() for k, v in self._term_reasons.items()})
        self.extras["log"] = log
        return reward

    def _reset_idx(self, env_ids: Sequence[int] | None):
        if env_ids is None:
            env_ids = self.robot._ALL_INDICES
        super()._reset_idx(env_ids)
        k, dev, c = len(env_ids), self.device, self.cfg

        # 관절: a_stand ± noise, 정책 순서 → 관절 순서
        joint_pos = self.robot.data.default_joint_pos[env_ids].clone()
        joint_pos[:, self._a2j] = self._a_stand + sample_uniform(
            -c.init_joint_noise, c.init_joint_noise, (k, ol.NUM_ACTIONS), dev)
        self.robot.write_joint_state_to_sim(joint_pos, torch.zeros_like(joint_pos), None, env_ids)

        # 루트: env 원점 + (0, 0, init_height ± noise), roll/pitch ± noise, yaw 0, 속도 0
        root = self.robot.data.default_root_state[env_ids].clone()
        root[:, :3] = self.scene.env_origins[env_ids]
        root[:, 2] += c.init_height + sample_uniform(-c.init_height_noise, c.init_height_noise, k, dev)
        roll = sample_uniform(-c.init_tilt_noise, c.init_tilt_noise, k, dev)
        pitch = sample_uniform(-c.init_tilt_noise, c.init_tilt_noise, k, dev)
        root[:, 3:7] = quat_from_euler_xyz(roll, pitch, torch.zeros(k, device=dev))
        root[:, 7:] = 0.0
        self.robot.write_root_pose_to_sim(root[:, :7], env_ids)
        self.robot.write_root_velocity_to_sim(root[:, 7:], env_ids)

        # progress 의 기준점. root_pos_w 는 다음 update 전까지 갱신되지 않으므로 방금 쓴 값으로 (env 로컬)
        self._potentials[env_ids] = mdp.potential(root[:, :3] - self.scene.env_origins[env_ids], self._p_target)

        # 이력 리셋은 반드시 여기 — 관측은 리셋 *뒤에* 계산된다 (§9.3)
        ol.reset_history(self._rot_his, self._act_his, env_ids)
        self._skip_push[env_ids] = True

    def _get_observations(self) -> dict:
        obs = ol.push(self._rot_his, self._act_his,
                      ol.wxyz_to_xyzw(self._quat_pre), self._raw_act,
                      skip_mask=self._skip_push)
        self._skip_push[:] = False
        return {"policy": obs}
```

**configclass 주의:** `reward_weights: mdp.WalkRewardWeights = mdp.WalkRewardWeights()` 가 `configclass` 의 필드 처리(`to_dict`/`validate`)에서 거부되면 — Kit 없이는 미리 확인 불가 — 가중치 8개를 `rew_progress = 30.0` … 평면 float 필드로 두고 `__init__` 에서 `mdp.WalkRewardWeights(progress=cfg.rew_progress, …)` 로 조립한다. 그 외의 실패는 계획 밖 → 멈추고 보고.

- [ ] **Step 4: 통과 확인**

```bash
pytest isaac/tests/test_env_smoke.py --isaac -v
pytest isaac --isaac -q
```

Expected: 3 passed. 전체 Kit 실행에서 `test_asset_build` 와 같은 세션에 떠도 `SimulationContext` 충돌 없음. 첫 실행은 USD 빌드(캐시) 때문에 느릴 수 있다.

- [ ] **Step 5: Commit**

```bash
git add isaac/chair_rl/walk_env.py isaac/tests/test_env_smoke.py
git commit -m "feat[isaac]: walk_env — Chair-Walk-Direct-v0 DirectRLEnv, 리셋/스텝 스모크

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 스모크 — a2j 방향, 종료→리셋 첫 관측, 실기 순서 짝 (Kit)

**Files:**
- Test: `isaac/tests/test_env_smoke.py` (테스트 3개 추가)
- Modify: `isaac/chair_rl/walk_env.py` — 테스트가 드러내는 버그가 있을 때만

**Interfaces:**
- Consumes: Task 2 의 `env` 픽스처, `env.robot`, `env._a2j`, `env.reset_buf`, `env.cfg.init_*`.

- [ ] **Step 1: 실패하는 테스트 작성**

`isaac/tests/test_env_smoke.py` 끝에 추가:

```python
def test_a2j_routes_policy_index_to_named_joint(env):
    """정책 인덱스 k 에만 목표각을 주면 POLICY_JOINT_NAMES[k] 관절만 그 목표를 받는다.
    지령(joint_pos_target)은 물리 없이 정확히 확인되고, 실제 위치는 방향만 본다."""
    base = torch.tensor(mdp.A_STAND, device=env.device).expand(env.num_envs, -1)
    for k, name in enumerate(ol.POLICY_JOINT_NAMES):
        env.reset()
        act = base.clone()
        act[:, k] += 0.4
        for _ in range(5):                         # 0.5 s
            env.step(act)
        j = env.robot.joint_names.index(name)
        tgt = env.robot.data.joint_pos_target
        torch.testing.assert_close(tgt[:, j], act[:, k], atol=1e-6, rtol=0)
        others = [i for i in range(6) if i != j]
        torch.testing.assert_close(tgt[:, others], base[:, [ol.POLICY_JOINT_NAMES.index(env.robot.joint_names[i]) for i in others]],
                                   atol=1e-6, rtol=0)
        moved = env.robot.data.joint_pos[:, j] - env.robot.data.default_joint_pos[:, j]
        assert (moved > 0.2).all(), (name, moved)


def test_history_pairs_pre_step_quat_with_raw_action(env):
    """실기 순서 재현: index 0 = (a_t, a_t 작용 *전* 쿼터니언), 액션은 클립 전 raw (§3, §9.3)."""
    env.reset()
    q_pre = ol.wxyz_to_xyzw(env.robot.data.root_quat_w).clone()
    act = torch.full((env.num_envs, ol.NUM_ACTIONS), 1.5, device=env.device)   # 한계(0.8727) 밖
    obs, _, terminated, _, _ = env.step(act)
    alive = ~env.reset_buf
    assert alive.any()
    o = obs["policy"][alive]
    torch.testing.assert_close(o[:, 0:4], q_pre[alive], atol=1e-6, rtol=0)     # 작용 전 q
    assert torch.all(o[:, 4:8] == torch.tensor([0.0, 0.0, 0.0, 1.0], device=env.device))
    assert torch.all(o[:, 16:22] == 1.5)                                       # raw, 클립 안 됨
    assert torch.all(o[:, 22:40] == ol.ACT_INIT)
    # 관절 목표는 클립됐다
    assert env.robot.data.joint_pos_target.abs().max() <= ol.ACTION_LIMIT + 1e-6


def test_terminated_env_resets_with_init_history_and_death_reward(env):
    """env 3 을 뒤집어 놓으면 그 스텝에 terminated → 보상 = death(−1) 로 대체 → 리셋 →
    첫 관측은 리셋값 그대로. 다른 env 는 이력이 이어진다."""
    env.reset()
    env.step(_rand_actions(env))
    flipped = torch.tensor([[0.0, 1.0, 0.0, 0.0]], device=env.device)          # x 축 180° (w,x,y,z)
    pose = torch.cat([env.robot.data.root_pos_w[3:4], flipped], dim=1)
    env.robot.write_root_pose_to_sim(pose, env_ids=torch.tensor([3], device=env.device))
    obs, rew, terminated, truncated, _ = env.step(_rand_actions(env))
    assert terminated[3] and not truncated[3]
    assert rew[3].item() == pytest.approx(env.cfg.reward_weights.death)
    o = obs["policy"]
    assert torch.all(o[3, :16].view(4, 4)[:, :3] == 0.0) and torch.all(o[3, :16].view(4, 4)[:, 3] == 1.0)
    assert torch.all(o[3, 16:] == ol.ACT_INIT)
    assert env.episode_length_buf[3] == 0
    survivors = (~env.reset_buf).nonzero().squeeze(-1)
    assert (o[survivors, 16:22] != ol.ACT_INIT).any()                          # 이력이 이어짐
```

- [ ] **Step 2: 실행**

```bash
pytest isaac/tests/test_env_smoke.py --isaac -v
```

Expected: Task 2 의 구현이 맞으면 전부 PASS. 실패하면 그 실패가 곧 버그 — `walk_env.py` 를 고친다(계획 안). 예상 가능한 것: `joint_pos_target` 이 리셋 시 갱신되지 않아 첫 비교가 어긋남 → 테스트를 `set_joint_position_target` 이후 값으로만 비교(이미 그렇다). 뒤집힌 env 가 접지 관통으로 물리 폭발 → NaN 이면 `flipped` 대신 `tilt` 만 걸리는 90° 회전(`[0.7071, 0.7071, 0, 0]`) 으로 낮춘다.

- [ ] **Step 3: 통과 확인 + 전체 + diff 규모**

```bash
pytest isaac --isaac -q
pytest isaac -q
git diff --shortstat main
```

Expected: Kit 6 + CPU 전부 PASS. **diff 가 400 을 넘으면 멈추고 분할안(Task 1–2 / Task 3 커밋 경계)을 제시한다.**

- [ ] **Step 4: Commit**

```bash
git add isaac/tests/test_env_smoke.py isaac/chair_rl/walk_env.py
git commit -m "test[isaac]: walk_env — a2j 방향, 실기 순서 (raw 액션·작용 전 q) 짝, 종료→리셋 첫 관측

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: PR

- [ ] **Step 1: 푸시**

```bash
git push -u origin feat/12-walk-env
```

- [ ] **Step 2: PR 생성** — `gh pr create --base main`, 제목 `feat[isaac]: walk_env — Chair-Walk-Direct-v0 DirectRLEnv 와 스모크 (#12)`. 본문에 반드시:

**이 코드가 틀렸다면 어떻게 틀렸을지**
1. **`_quat_pre` 짝짓기가 실기와 어긋날 수 있다.** 실기 index 0 을 (a_t, a_t 작용 전 q) 로 읽었다. 저자의 *학습* env 가 Isaac 자연 순서(작용 후 q)였다면 우리 정책은 실기에서는 맞고 저자 정책과의 비교(§7 기준선)에서는 한 주기 어긋난다. 반증: `models/walk.onnx` 를 이 env 에서 재생했을 때 에피소드 길이 분포.
2. **`init_height = 0.101` 은 발이 정확히 접지하는 높이라는 가정.** 관통이면 PhysX 가 첫 스텝에 밀어 올려 progress/height 항에 스파이크, 떠 있으면 낙하. 증상: 첫 스텝 `rew/height` 나 `term/ground` 의 이상. §5 의 "base 는 접지 상태" 를 z 하나로 근사했다.
3. **`prev_actions = act_his[:, 0]` 이라 리셋 직후 첫 action_cost = ‖a₀ − 1.0‖².** 실기 초기 이력(1.0)과 같은 값이지만 저자가 action_cost 에 별도 `prev_action` 버퍼(0 초기화)를 썼다면 첫 스텝 페널티가 다르다. 학습에 미치는 영향은 스텝 1개.
4. **env 로컬 좌표 변환이 `env_origins` z 에 의존.** 평지라 z=0 이지만 지형을 넣으면 height/ground 종료가 원점 z 만큼 어긋난다. 현재 범위에선 성립.
5. **`a2j` 는 `find_joints(preserve_order=True)` 의 계약에 기댄다.** Isaac Lab 이 이 플래그의 의미를 바꾸면 `test_a2j_routes_policy_index_to_named_joint` 가 잡는다.
6. **`_skip_push` 는 `_reset_idx` → `_get_observations` 가 같은 스텝에 1:1 로 불린다는 가정.** `DirectRLEnv` 가 중간에 `_get_observations` 를 한 번 더 부르는 경로(예: `render`)가 생기면 마스크가 일찍 꺼진다.

---

## Self-Review

**Spec coverage:** §1 태스크 ID·등록 → Task 1 ✓ (agent entry point 는 명시적 축소). §3 (x,y,z,w) 변환·타이밍 짝·raw 액션 → Task 2 구현 + Task 3 테스트 ✓. §5 walk 초기 분포(a_stand±0.02, z±2 mm, roll/pitch±0.02, yaw 0) → Task 2 `_reset_idx` ✓. §9.3 훅 순서·이력 리셋 위치·potentials 재초기화·a2j 단일 출처 → Task 2 ✓. §9.4 종료 → `mdp.walk_terminated` 호출 ✓. §9.5 Cfg 필드(dt, decimation, episode_length_s, spaces, scene) ✓. §9.6 `test_env_smoke` (16 env × 50 스텝, NaN 없음, 리셋 직후 규약값, a2j 로 움직임) → Task 2+3 ✓. `keyframes.STANDING_SIM` 은 스펙 표기와 달리 존재하지 않아 `mdp.A_STAND` 로 대체하고 CPU 테스트가 `keyframes` 변환과 대조한다 — 계획의 명시적 이탈.

**Type consistency:** `ol.push(rot_his, act_his, quat_xyzw, action, skip_mask)` ↔ `_get_observations` 호출; `mdp.walk_reward_terms(root_pos, root_quat, potentials, actions, prev_actions, joint_vel, dt, p_target) -> (terms, new_pot)` ↔ `_get_rewards`; `mdp.walk_total(terms, w, terminated)` ↔ `reset_terminated`; `mdp.walk_terminated(root_pos, root_quat) -> (Tensor, dict)` ↔ `_get_dones` 언패킹; `mdp.potential(root_pos, p_target, dt=CONTROL_DT)` ↔ `_reset_idx`; `articulation_cfg(usd_path, prim_path, spawn_height, joint_pos, effort_limit)` ↔ `_setup_scene`; 테스트의 `env._potentials`, `env.reset_buf`, `env.cfg.init_height`, `env.cfg.reward_weights.death` ↔ Task 2 속성.

**알려진 미확정:** `configclass` 가 frozen dataclass 필드를 받는지 (Task 2 fallback 명시); 뒤집힌 루트 쓰기가 물리적으로 안정한지 (Task 3 Step 2 대안 명시).
